/**
 * 构造统一的Input格式
 */
import { PureInput, Input, SupportEvent } from '@any-touch/shared';
import Adapter from './adapters/Abstract';
export default class {
    adapter: Adapter;
    id: number;
    prevInput?: PureInput;
    activeInput?: PureInput;
    startInput?: PureInput;
    startMultiInput?: PureInput;
    constructor();
    transform(event: SupportEvent): Input | void;
}
/**
 * 是否每个触点的target都是currentTarget的子元素
 * 过滤第一个点非绑定元素, 但是第二个触点是绑定元素的情况
 * @param baseInputWithoutId 输入
 */
