import AnyTouch from './index';
import { Input } from '@any-touch/shared';
/**
 * 触发自定义和dom事件
 * @param at AnyTouch实例
 * @param payload 数据
 */
export default function emit2(at: AnyTouch, payload: Record<string, any> & Input): void;
